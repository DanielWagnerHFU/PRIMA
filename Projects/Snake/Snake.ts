namespace Snake {
    import ƒ = FudgeCore;

    class Snake {
        greeting: string;

        constructor(message: string) {
            this.greeting = message;
        }

        x(): void {
            
        }
    }
}